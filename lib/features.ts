/** Presentation-only feature flags — not authoritative security policy. */

/** Agent mode placeholder visibility in chat chrome (default hidden for MVP). */
export const NEXUS_SHOW_AGENT_PLACEHOLDER = false;
