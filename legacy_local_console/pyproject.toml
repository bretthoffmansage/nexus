[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[project]
name = "nexus-console"
description = "legacy local console UI and Nexus Gateway API (reformed Odysseus codebase)"
readme = "README.md"
